<?php

namespace Skuth\DB\Sql;
use Skuth\Model;

class Painel {
  const SESSION = "logado";

  public function login($user, $pass) {
    $user = trim(strip_tags($user));
    $pass = trim(strip_tags($pass));

    
  }
}

?>